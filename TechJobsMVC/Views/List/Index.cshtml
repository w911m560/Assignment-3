<div class="container body-content">
    <h1>@ViewBag.title</h1>
    <h2 class="centered">View Jobs By Category</h2>
</div>

<table class="centeredTable">
    <tr>
        @foreach (var column in ViewBag.columns)
        {
            @if (column.Value == "All")
            {
                <th><a asp-controller="List" asp-action="Jobs" asp-route-column="All" asp-route-value="All">All</a></th>
            }
            else
            {
            <th>@column.Value</th>
            }
        }
    </tr>
    <tr>
        <td>
            <ul>
                @foreach (var category in ViewBag.tableChoices)
                {
                    @foreach (var item in category.Value)
                    {
                            <li>
                                <a asp-controller="List" asp-action="Jobs" asp-route-column="@category.Key" asp-route-value="@item">@item</a>
                            </li>
                    }
                }
            </ul>
        </td>

        @foreach (var category in ViewBag.tableChoices)
        {
            <td>
                <ul>
                    @foreach (var item in category.Value)
                    {
                        <li>
                            <a asp-controller="List" asp-action="Jobs" asp-route-column="@category.Key" asp-route-value="@item">@item</a>
                        </li>
                    }
                </ul>
            </td>
        }
    </tr>
        </table>
